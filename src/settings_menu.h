#ifndef __CG__SETTINGS_MENU_H__
#define __CG__SETTINGS_MENU_H__

#include <stdbool.h>

// returns true if "back" button is pressed (to exit the settings)
bool cgSettingsMenuDraw();

#endif // __CG__SETTINGS_MENU_H__
